export type SignupFormValues = {
  fName: string;
  lName: string;
  email: string;
  handlePersonalData: boolean;
};

export type Category = {
  id: string;
  label: string;
  imageUrl: string;
  description: string;
};
